# Application
spring.application.name=ZuluGshop
server.port=${PORT:8081}

# MongoDB
#spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/zulugshop}
spring.data.mongodb.uri=mongodb+srv://rizsameer001_db_user:apyzUNvvS0p0318K@cluster0.xbnnnsq.mongodb.net/springbootzuluecom

spring.data.mongodb.database=zulugshop

# JWT
jwt.secret=${JWT_SECRET:ZuluGshop_SuperSecret_Key_2024_Change_In_Production}
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# CORS
cors.allowed-origins=${FRONTEND_URL:http://localhost:5173}

# Logging
logging.level.com.zulugshop=DEBUG
logging.level.org.springframework.security=INFO

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
